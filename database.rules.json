{
  "rules": {
    "election": {
      ".read": true,
      ".write": "auth != null",
      "candidates": {
        "$id": {
          "votes": { ".validate": "newData.isNumber() && newData.val() >= 0" },
          "name": { ".validate": "newData.isString() && newData.val().length <= 30" },
          "imageUrl": { ".validate": "newData.isString() && newData.val().length <= 500" },
          "id": { ".validate": "newData.isNumber()" }
        }
      },
      "meta": {
        "title": { ".validate": "newData.isString() && newData.val().length <= 80" },
        "subtitle": { ".validate": "newData.isString() && newData.val().length <= 80" },
        "locked": { ".validate": "newData.isBoolean()" }
      },
      "log": { ".indexOn": ["ts"] },
      "presence": {
        ".read": true,
        "$sid": {
          ".write": true,
          ".validate": "newData.hasChild('ts')"
        }
      }
    },
    "arena_rooms": {
      "$pin": {
        ".read": "auth != null",
        ".write": "auth != null && ((!data.exists() && newData.child('hostUid').val() == auth.uid) || (data.exists() && data.child('hostUid').val() == auth.uid))",
        "players": {
          "$playerId": {
            ".write": "auth != null && $playerId == auth.uid && ((!data.exists() && root.child('arena_rooms').child($pin).child('status').val() == 'waiting' && root.child('arena_rooms').child($pin).child('expiresAt').val() > now && newData.child('score').val() == 0) || (data.exists() && root.child('arena_rooms').child($pin).child('status').val() == 'question' && now <= root.child('arena_rooms').child($pin).child('questionStartedAt').val() + root.child('arena_rooms').child($pin).child('questionDurationMs').val() + 500 && data.child('answerQuestionIndex').val() != root.child('arena_rooms').child($pin).child('currentQuestionIndex').val() && newData.child('answerQuestionIndex').val() == root.child('arena_rooms').child($pin).child('currentQuestionIndex').val() && newData.child('currentAnswer').val() >= 0 && newData.child('currentAnswer').val() <= 3 && newData.child('isCorrect').val() == (newData.child('currentAnswer').val() == root.child('arena_rooms').child($pin).child('currentCorrectIndex').val()) && ((newData.child('isCorrect').val() == true && newData.child('score').val() - data.child('score').val() >= 100 && newData.child('score').val() - data.child('score').val() <= 250 && (newData.child('score').val() - data.child('score').val() - 100) % 10 == 0 && root.child('arena_rooms').child($pin).child('questionStartedAt').val() + root.child('arena_rooms').child($pin).child('questionDurationMs').val() - now + 500 > (newData.child('score').val() - data.child('score').val() - 110) * 100) || (newData.child('isCorrect').val() == false && newData.child('score').val() == data.child('score').val())) && newData.child('answeredAt').val() >= root.child('arena_rooms').child($pin).child('questionStartedAt').val() - 2000 && newData.child('answeredAt').val() <= now + 2000 && newData.child('id').val() == data.child('id').val() && newData.child('nickname').val() == data.child('nickname').val() && newData.child('avatar').val() == data.child('avatar').val() && newData.child('joinedAt').val() == data.child('joinedAt').val()))",
            ".validate": "newData.hasChildren(['id', 'nickname', 'avatar', 'score', 'joinedAt']) && newData.child('id').val() == $playerId",
            "id": { ".validate": "newData.isString() && newData.val() == $playerId" },
            "nickname": { ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 12" },
            "avatar": { ".validate": "newData.isNumber() && newData.val() >= 1 && newData.val() <= 8" },
            "score": { ".validate": "newData.isNumber() && newData.val() >= 0" },
            "joinedAt": { ".validate": "newData.isNumber() && newData.val() > 0" },
            "answerQuestionIndex": { ".validate": "newData.isNumber() && newData.val() >= 0" },
            "currentAnswer": { ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 3" },
            "isCorrect": { ".validate": "newData.isBoolean()" },
            "answeredAt": { ".validate": "newData.isNumber() && newData.val() > 0" },
            "$other": { ".validate": false }
          }
        }
      }
    }
  }
}
